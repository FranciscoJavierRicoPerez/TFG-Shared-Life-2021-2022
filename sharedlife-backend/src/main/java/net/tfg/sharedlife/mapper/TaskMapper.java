package net.tfg.sharedlife.mapper;

//@Mapper
public interface TaskMapper {
    /* @Mapping(source = "taskdto.id", target = "task.id")
    @Mapping(source = "taskdto.title", target = "task.title")
    @Mapping(source = "taskdto.startDate", target = "task.startDate")
    @Mapping(source = "taskdto.endDate", target =  "task.endDate")
    @Mapping(source = "taskdto.finished", target = "task.finished")
    @Mapping(source = "taskdto.weekTask", target = "task.weekTask")
    @Mapping(source = "taskdto.homeRoom", target = "task.homeRoom")
    @Mapping(source = "taskdto.user", target = "task.user")
    Task taskDTOtoTask(TaskDTO taskdto);


    TaskDTO taskToTaskDTO(Task task);*/
}
